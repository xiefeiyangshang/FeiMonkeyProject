package com.whatyplugin.imooc.ui.showmooc;


import android.content.Context;
import android.util.AttributeSet;
import android.view.LayoutInflater;
import android.widget.LinearLayout;
import cn.com.whatyplugin.mooc.R;

public class TeacherLayout extends LinearLayout {
    public TeacherLayout(Context context) {
        super(context);
        this.init(context);
    }

    public TeacherLayout(Context context, AttributeSet attr) {
        super(context, attr);
        this.init(context);
    }

    private void init(Context context) {
        this.addView(LayoutInflater.from(context).inflate(R.layout.teacher_layout, null));
    }
}

